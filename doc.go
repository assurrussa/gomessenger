// Package messenger provides typed commands, local queries, and events with
// explicit descriptors, GoBus dispatch, and transport-neutral one-way delivery
// contracts. Queries are process-local request/reply calls and never use the
// wire envelope or durable routes.
package messenger
